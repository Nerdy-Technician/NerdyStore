services:
  jackett:
    image: ghcr.io/linuxserver/jackett
    container_name: jackett
    restart: always
    ports:
      - "9117:9117"
    volumes:
      - ./config:/config
      - ./downloads:/downloads
    environment:
      - PUID=1026
      - PGID=100
      - AUTO_UPDATE=true
